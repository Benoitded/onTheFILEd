import React, { useEffect, useState } from "react";
import "./Headline.css";

export default function Headline({ currentAccount, chain, setChain }) {
  return (
    <main>
      <div className="mainHeadline">Headline</div>
    </main>
  );
}
